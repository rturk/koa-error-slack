{
  "name": "koa-error-slack",
  "version": "0.0.2",
  "description": "Koa - Send server errors to Slack",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "npm run build",
    "build": "rm -rf dist/* && babel src --ignore *.spec.js --out-dir dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rturk/koa-error-slack.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/rturk/koa-error-slack/issues"
  },
  "homepage": "https://github.com/rturk/koa-error-slack#readme",
  "dependencies": {
    "node-slack": "0.0.7"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
